services:
  - type: web
    name: flask-api
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    workingDir: backend
    plan: free
